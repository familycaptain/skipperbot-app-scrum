"""Skipperbot Scrum App.

A daily-standup / PM-daily-update tracker. The platform's Goals/PM runner
writes a personalized set of scrum items per person each morning; this app
persists them, surfaces them in chat + the desktop UI, and records responses
(optionally rolling a linked task's status forward).
"""
